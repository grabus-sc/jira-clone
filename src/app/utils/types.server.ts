export type RegisterForm = {
    email: string
    password: string
    name: string
    firstName: string
    lastName: string
    color: string
  }

  export type LoginForm = {
    email: string
    password: string
  }